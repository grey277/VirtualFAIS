<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ToolBarStyle" parent="Base.Theme.AppCompat">
        <item name="android:textColorPrimary">@color/toolbar_title_color</item>
        <item name="android:textColorSecondary">@color/toolbar_subtitle_color</item>
    </style>

    <style name="PopUp" >
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>

        <!-- hides top bar in popup -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>

    <declare-styleable name="BottomMenuItemStyle">
        <attr format="string" name="title"/>
        <attr format="reference" name="normalImage"/>
        <attr format="reference" name="selectedImage"/>
        <attr format="color" name="normalTitleColor"/>
        <attr format="color" name="selectedTitleColor"/>
    </declare-styleable>

    <style name="ButtonTheme" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorPrimary</item>
    </style>

    <style name="ProgressDialogTheme" parent="Base.Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/black50</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>


</resources>
